%% Contest-problem template (a FRAGMENT, not a full document).
%%
%% Used by `rbx contest st b` to render each problem before it is
%% `\subimport`-ed into the contest task sheet (`contest.rbx.tex`). It shares
%% its body with the standalone template via `_problem-body.rbx.tex`, so a
%% problem looks identical whether built on its own or inside the contest.
%- include "_problem-body.rbx.tex"
