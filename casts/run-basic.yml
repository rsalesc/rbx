fixture: ab-problem
title: Running solutions with rbx run
setup:
  - rbx build
instructions:
  - rbx run
expect_contains:
  # If rbx's run output changes shape, the recording fails here rather than
  # silently publishing a cast of something else.
  - sols/main.cpp
  - sols/overflow.cpp
  - Timing summary
