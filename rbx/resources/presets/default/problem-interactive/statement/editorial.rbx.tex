%- block legend
Keep a candidate interval $[lo, hi]$, initially $[1, N]$, and always guess its
midpoint. A guess either hits $S$ or halves the interval, so
$\lceil \log_2 (N + 1) \rceil \leq \VAR{vars.Q.max}$ guesses always suffice.
%- endblock
