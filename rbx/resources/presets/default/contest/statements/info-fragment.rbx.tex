%% Intentionally empty. The info sheet (`info.rbx.tex`) reads each problem's
%% limits directly from the `problems` list and does not `\subimport` problem
%% bodies — but rbx contest statements must declare a `contestProblemTemplate`,
%% so this is the no-op fragment it renders per problem.
