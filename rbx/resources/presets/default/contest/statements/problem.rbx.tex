%% Standalone problem template (a FULL LaTeX document).
%%
%% Used by `rbx st b` to render a single problem on its own. The contest owns
%% this template (statements v2): when you build a problem standalone, rbx
%% borrows the `standaloneProblemTemplate` from the contest statement whose
%% (language, variant) matches the problem statement.
\documentclass[a4paper,11pt]{article}

\def\lang{\VAR{lang}}
\usepackage{icpc}
\usepackage{import}

%- if problem.blocks.preamble is defined
	\VAR{problem.blocks.preamble}
%- endif

\begin{document}

%- include "_problem-body.rbx.tex"

\end{document}
